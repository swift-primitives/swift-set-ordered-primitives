# ``Set_Ordered_Primitives``

@Metadata {
    @DisplayName("Set Ordered Primitives")
    @TitleHeading("Swift Primitives")
}

An insertion-ordered hash set generic over its storage column. `Set.Ordered` preserves the order in which elements were inserted while keeping O(1) average-case membership, with copyability flowing from the column.

## Topics
```
